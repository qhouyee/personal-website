import React from 'react';

export interface IExperienceProps { }

const Experience: React.FC<IExperienceProps> = (props: IExperienceProps) => {
  return (
    <main>
      <p>This is the experiences page</p>
    </main>
  );
};

export default Experience;